# Components

Shared UI lives here. Keep route-specific components close to their route in `src/app` when they are not reused.

- `layout/`: structural components such as headers, footers, and navigation.
- `sections/`: landing-page or content sections.
- `ui/`: small reusable primitives.

